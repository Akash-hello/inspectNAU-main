- For dynamic UI layouts (e.g., lists in a ScrollView), prioritize automatic sizing using `ContentSizeFitter` and `LayoutGroup` components. Avoid setting fixed `preferredHeight` or `minHeight` on `LayoutElement` components, as this can interfere with dynamic resizing.